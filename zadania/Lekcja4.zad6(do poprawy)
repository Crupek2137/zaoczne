using System;
using System.Collections.Generic;

class Program
{
    static void Main()
    {
        Dictionary<string, int> slowa = new Dictionary<string, int>();



            Console.WriteLine("Podaj zdanie");
            string zdanie = Console.ReadLine();
            string[] wyrazy = zdanie.Split(" ");
            foreach(string x in wyrazy ){
                if (slowa.ContainsKey(nazwa))
                {
                    slowa[nazwa] += 1;
                }
                else
                {
                    slowa.Add(nazwa, 1); 
                }
                Console.WriteLine($"Dodano slowo: {nazwa}, Ilosc: {ilosc}");
            }
                Console.WriteLine("Ilosc slow:");
                foreach (var slowo in slowa)
                {
                    Console.WriteLine($"slowo: {slowo.Key}, Ilosc: {slowo.Value}");
                }
            
            
    }
}
